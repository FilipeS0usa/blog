services:
  jekyll:
    image: ruby:3.3
    working_dir: /site
    volumes:
      - .:/site
      - bundle-cache:/usr/local/bundle
    network_mode: host
    command: >
      bash -c "bundle install && bundle exec jekyll serve --host 0.0.0.0 --livereload"
volumes:
  bundle-cache:
